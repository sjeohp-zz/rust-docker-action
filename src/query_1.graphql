mutation RequestReviews($input: { $pullRequestId: ID!, $teamIds: [ID!], $userIds: [ID!] }) {
  requestReviews(pullRequestId: $pullRequestId, teamIds: $teamIds, userIds: $userIds) {
    clientMutationId
    pullRequest
  }
}
